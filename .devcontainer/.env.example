# =============================================================================
# Meow Machine - Dev Container Configuration
# =============================================================================
#
# Copy this file to .env and customize for your setup.
# The .env file is gitignored and won't be committed.
#
# =============================================================================

# Base development container image
# ---------------------------------
# Set this to your personal dev-container image with all your tools.
# If not set, falls back to ubuntu:25.10 (minimal, no tools pre-installed).
#
# Examples:
#   BASE_IMAGE=dev-container:latest          # Local image
#   BASE_IMAGE=ghcr.io/yourname/dev-container:latest  # Remote registry
#
BASE_IMAGE=dev-container:latest

# =============================================================================
# Volumes
# =============================================================================

# Home directory volume
# - Leave as default: creates project-specific 'meow-home' volume
# - Set to shared volume: reuse your main dev environment
#
# HOME_VOLUME=meow-home                    # Project-specific (default)
# HOME_VOLUME=acfs-setup_acfs-home         # Reuse existing ACFS volume
# HOME_VOLUME_EXTERNAL=true                # Required if using external volume

# =============================================================================
# Paths
# =============================================================================

# SSH directory (for git operations)
# SSH_DIR=~/.ssh

# =============================================================================
# Ports (change if conflicts with other containers)
# =============================================================================

# PORT_8080=8080
# PORT_3000=3000

# =============================================================================
# Resources
# =============================================================================

# CPU_LIMIT=4
# MEM_LIMIT=8G

# =============================================================================
# Environment
# =============================================================================

# TZ=America/New_York
